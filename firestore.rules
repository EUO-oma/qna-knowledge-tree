rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /nodes/{nodeId} {
      allow read: if true;
      allow create, update, delete: if true;
    }
  }
}
